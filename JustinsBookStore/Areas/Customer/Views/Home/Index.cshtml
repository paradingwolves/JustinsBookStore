@model IEnumerable<JustinsBooks.Models.Product>
@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
<div class="row pb-3 backgroundWhite">

    @foreach (var Product in Model)
    {
        <div class="col-lg-3 col-md-6">
            <div class="row p-2">
                <div class="col-12  p-1" style="border:1px solid #008cba; border-radius: 5px;">
                    <div class="card" style="border:0px;">
                        <img src="@Product.ImageUrl" class="card-img-top rounded" />
                        <div class="pl-1">
                            <p class="card-title h5"><b style="color:#2c3e50">@Product.Title</b></p>
                            <p class="card-title text-primary">by <b>@Product.Author</b></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p>List Price: <strike><b class="">$ @Product.ListPrice.ToString("0.00")</b></strike></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p style="color:maroon">As low as: <b class="">$@Product.Price100.ToString("0.00")</b></p>
                        </div>
                    </div>
                    <div>
                        <a asp-area="Admin" asp-controller="Product" asp-action="Upsert" class="btn btn-primary form-control" asp-route-id="@Product.Id">Details</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div>


@*<br />
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary">Product List </h2>
        </div>
        <div class="col-6 text-right">
            <a class="btn btn-primary" asp-area="Admin" asp-controller="Product" asp-action="Upsert"><i class="fas fa-plus"></i> &nbsp; Create New Product</a>
        </div>
    </div>

    <br />
    <div class="p-4 border rounded">
        <table class="table table-dark table-bordered" id="tblData" style="width:100%">
            <thead class="thead-dark">
                <tr class="table-info">
                    <th>Title</th>
                    <th>ISBN</th>
                    <th>Price</th>
                    <th>Author</th>
                    <th>Category</th>
                    <th>Description</th>
                    <th>Image</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>

    @section Scripts{

        <script src="~/js/product.js"></script>
    } 
*@


@*<div class="row pb-3 backgroundWhite">

    @foreach (var product in Model)
    {
        <div class="col-lg-3 col-md-6">
            <div class="row p-2">
                <div class="col-12  p-1" style="border:1px solid #008cba; border-radius: 5px;">
                    <div class="card" style="border:0px;">
                        <img src="@product.ImageUrl" class="card-img-top rounded" />
                        <div class="pl-1">
                            <p class="card-title h5"><b style="color:#2c3e50">@product.Title</b></p>
                            <p class="card-title text-primary">by <b>@product.Author</b></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p>List Price: <strike><b class="">$@product.ListPrice.ToString("0.00")</b></strike></p>
                        </div>
                    </div>
                    <div>
                        <a asp-action="Details" class="btn btn-primary form-control" asp-route-id="@product.Id">Details</a>
                    </div>
                </div>
            </div>
        </div>
    }
</div> *@